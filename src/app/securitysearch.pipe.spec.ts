import { SecuritysearchPipe } from './securitysearch.pipe';

describe('SecuritysearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SecuritysearchPipe();
    expect(pipe).toBeTruthy();
  });
});
